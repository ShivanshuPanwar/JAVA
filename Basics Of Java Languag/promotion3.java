public class promotion3 {
    public static void main(String args[])
    {
        byte b = 5;
        byte a = (byte) (b * 2);
        // byte b = 5;
        // byte a = b * 2;  // b * 2 is a expression. it converts byte into int. therfore byte cannot store int  
        System.out.println(a);
    }
}
